<% if user.errors.any? %>
  <div class="auth__form-errors">
    <ul>
      <% user.errors.attribute_names.each do |key| %>
        <li><%= user.errors[key].first %></li>
      <% end %>
    </ul>
  </div>
<% end %>
